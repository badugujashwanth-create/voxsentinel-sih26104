[pytest]
# Two test roots: backend API tests and the root-level ml/ chunker tests.
testpaths = backend/tests tests
pythonpath = . backend
addopts = -q
markers =
    integration: needs the downloaded AASIST checkpoint and the ML runtime
