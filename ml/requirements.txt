# VoxSentinel ML subsystem (spoof detection).
#
# Kept separate from backend/requirements.txt on purpose: the demo backend must
# stay free of a torch runtime until MLRiskProvider lands in a later task.
#
# torch MUST come from the PyTorch CPU index, otherwise pip/uv resolves the
# CUDA build from PyPI and pulls several GB of unused NVIDIA wheels:
#
#   uv pip install --index-url https://download.pytorch.org/whl/cpu torch==2.9.1
#   uv pip install -r ml/requirements.txt
#
# GPU users can install a CUDA torch build instead; the detector takes a
# --device argument. Everything here runs comfortably on CPU.

torch==2.9.1
numpy==2.3.4
scipy==1.16.3
soundfile==0.13.1

# Tests
pytest==9.1.1
